const Tags = ({ title }: any) => {
    return (
      <div className="px-2 py-1 border-2 border-sky-200 w-fit text-sm font-semibold text-blue-800 bg-gradient-to-r from-sky-100 via-slate-50 to-sky-100 cursor-pointer">
        {title}
      </div>
    );
  };
  
  export default Tags;
  